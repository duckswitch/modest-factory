"""
Setup file describing how to install.
"""

from setuptools import setup

setup(
    name=u'simplefactory',    # This is the name of your PyPI-package.
    version=u'0.1',                          # Update the version number for new releases
    packages=[u'simplefactory'],
    install_requires=[]
)
